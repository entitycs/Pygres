# Pygres
A lightweight, Pydantic‑first storage layer for persisting typed tool inputs and outputs in PostgreSQL using JSONB. Designed for development workflows, reproducible analysis, and schema‑driven introspection without the overhead of a full ORM.
